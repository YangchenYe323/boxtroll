name: Release

on:
  push:
    tags:
      - 'v*'  # Triggers on version tags like v1.0.0

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
        shell: bash

      - name: Build Go binary
        run: |
          go build -o boxtroll.exe -ldflags "-s -w -X command.Version=${{ steps.version.outputs.VERSION }}" ./cmd/boxtroll
        shell: bash

      - name: Install go-msi
        run: |
          go install github.com/mh-cbon/go-msi@latest
        shell: bash

      - name: Set PATH for WiX Toolset
        run: |
          echo "C:\Program Files (x86)\WiX Toolset v3.11\bin" >> $GITHUB_PATH
        shell: bash
      
      - name: Go-MSI Check Env
        run: |
          go-msi check-env
        shell: bash

      - name: Go-MSI Check JSON
        run: |
          go-msi check-json installer/wix.json
        shell: bash

      - name: Build MSI
        run: |
          export TMP="$PWD/tmp"
          export TEMP="$TMP"
          mkdir -p "$TMP"
          go-msi make \
            --msi boxtroll-${{ steps.version.outputs.VERSION }}-amd64.msi \
            --version ${{ steps.version.outputs.VERSION }} \
            --arch amd64 \
            --src 'installer/templates/' \
            --path 'installer/wix.json'
        shell: bash

      - name: Create Release and Upload MSI
        uses: softprops/action-gh-release@v1
        with:
          files: ./boxtroll-${{ steps.version.outputs.VERSION }}-amd64.msi
          name: Release ${{ steps.version.outputs.VERSION }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}